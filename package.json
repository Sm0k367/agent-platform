{
  "name": "epic-tech-ai-agent",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "api": "cd apps/api && python -m uvicorn main:app --reload --port 8000",
    "web": "turbo run dev --filter=web"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "npm@10.9.7",
  "workspaces": ["apps/*", "packages/*"]
}
