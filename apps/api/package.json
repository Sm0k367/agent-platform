{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "build": "echo 'FastAPI build complete (no compile step)'",
    "clean": "rm -rf __pycache__ .pytest_cache"
  },
  "dependencies": {}
}
